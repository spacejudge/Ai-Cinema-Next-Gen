# PromptDirector AI

Generate professional cinematic prompts from rough video ideas using OpenAI's GPT-4.

## Setup

1. Clone the repo
2. Run `npm install`
3. Add `.env.local` with your OpenAI key:
```
OPENAI_API_KEY=sk-...
```
4. Run the app:
```
npm run dev
```